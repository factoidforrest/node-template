




angular.module('mobileGiftCardWebApp')
    .controller('RegisterCtrl',["$scope", "UserService", "ENV", function ($scope, userService, ENV) {

        $scope.apiRoot = ENV.apiRoot;

        $scope.awesomeThings = [

		    ];
    }]);

